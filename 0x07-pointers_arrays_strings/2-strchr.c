#include "main.h"
#include <string.h>

/**
 * _strchr - locates a character in a string.
 * @s: the string
 * @c: the character
 * Return: a pointer to c or NULL
 */

char *_strchr(char *s, char c)
{
if (s != NULL)
return (c);
}
