#include "main.h"
#include <string.h>

/**
 * create_array - create an array of chars
 * @size: the size of the char
 * @c: the char value
 * Return: a pointer to the areay or NULL
 */

char *create_array(unsigned int size, char c)
{
	int n;

	if (unsigned int size == 0)
	{
		printf('\0');
	}
	while (n = malloc(n * size_of(c))
		n++;

	return (n);
}
