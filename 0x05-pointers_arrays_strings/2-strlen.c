#include <string.h>
#include "main.h"

/**
 * _strlen - returns the length of a string
 * @l: the integer to be used
 * Return: 0
 */

int _strlen(char *s)
{
int l;

for (l = 0; s[l] != '\0'; l++)
{
return (l);
}

return (0);
}
