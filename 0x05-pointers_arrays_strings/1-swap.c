#include "main.h"

/**
 * swap_int - swaps the value of two integers
 * @m: the first integer
 * @n: the second integer
 * Return: 0
 */

void swap_int(int *a, int *b)
{
int *a = a;
int *b = b;

int temp = a;

a = b;
b = temp;
}
