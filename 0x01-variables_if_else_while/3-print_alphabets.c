#include <stdlib.h>
#include <stdio.h>

/**
 * main - print alphabet in lowercase and then uppercase
 * Return: Always 0 (success)
 */

int main(void)
{
char ch = 'a';
char d = 'A';

while (ch <= 'z')

while (d <= 'Z')
{
putchar(%c , ch);
ch++;
}
{putchar(%s , d);
d++;
}

return (0);
}
