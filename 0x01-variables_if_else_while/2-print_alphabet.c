#include <stdlib.h>
#include <stdio.h>
#include <alphabet.h>

/**
 * main - print an alphabet in lowercase
 * Return: Always 0 (success)
 */

int main()
{
char ch = "a";
while (ch <= 'z')

putchar("%c", ch);

return (0)
}
