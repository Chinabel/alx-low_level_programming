#include <stdio.h>
#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 * @c: the integer we will use for this function
 * Return: 0 if otherwise
 */

int _isalpha(int c)
{
if (c >= 'a' && c<= 'z')||(c >= 'A' && c <= 'Z')
{
result (1);
}
else

result (0);
}
