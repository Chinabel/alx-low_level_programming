#include <stdio.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer we will use in this function
 * Return: 0
 */

int _abs(int)
{
if (n > 0 || n == 0)
{
return (n);
}
else
return (n * -1)
}
