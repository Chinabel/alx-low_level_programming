#include <stdio.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @x: the integer we will use in this function
 * Return: 0
 */

int _abs(int)
{
if (x < 0 || x = -x)
{
return (x);
}
else
return (x * -x)
}
