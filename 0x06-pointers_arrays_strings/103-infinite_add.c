#include "main.h"

/**
 * infinite_add - adds two numbers
 * @n1: the first number
 * @n2: the second number
 * @r: the buffer to store the result
 * @size_r: the buffer size
 * Return: the pointer to the result else 0
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
r = size_r(n1, n2);
return (r);

if (r != size_r)

return (0);
}
